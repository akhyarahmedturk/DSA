#include<iostream>
#include<vector>
#include<map>
#include<string>
using namespace std;
void solve(vector<vetor<int>> &res,vector<int> &vec,int idx,int N){
    if(idx==N-1){
        res.push_back(temp);
        return;
    }
    else if()
    else{
        for(int i=idx+1)
    }
}
int main(){
    int N=1;
    vector<vector<int> res;
    if(N==1){
        vector<int> temp={0};
        for(int i=0;i<=9;i++){
            temp[0]=i;
            res.push_back(temp);
        }
    }
    else{
        vector<int> temp(9);
        solve(res,temp,0,N);
    }
    return 0;
}